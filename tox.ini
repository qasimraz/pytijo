[testenv:check]
commands = pytest --cov=./pytijo
deps =
    pytest
    coverage
    pytest-cov

[pytest]
python_files = test_*.py
norecursedirs = .tox

[testenv]
passenv = CI TRAVIS TRAVIS_*
deps = codecov
commands = codecov